import React from 'react';
import ReactDOM from 'react-dom/client';
import { AdultValidator } from './components/AdultValidator.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AdultValidator />
  </React.StrictMode>,
);
